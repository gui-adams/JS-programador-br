
/*
operador ternario é = ? 
esse operador ? é maior ou igual ? 
o if (teoria) é ?
o else é :
é usado para substituir o if em algumas questões
*/

var idade = 19;

var pode = idade >= 18 ? true : false; // se idade for maior ou igual 18 é true "se não" é falso

console.log(pode)
